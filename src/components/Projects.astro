---
import projects from "../json/projects.json";
import Card from "./Card.astro";
---

{
    Object.keys(projects).map((key) => {
        const project = projects[key];
        return <Card key={key} info={project} />;
    })
}
