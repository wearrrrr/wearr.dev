---
import teams from "../json/teams.json";
import Card from "./Card.astro";
---

{
    Object.keys(teams).map((key) => {
        const team = teams[key];
        return <Card key={key} info={team} />;
    })
}
